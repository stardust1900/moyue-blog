---
import { formatDate } from '../utils/posts';
interface Props {
  date: Date;
  class?: string;
}
const { date, class: className = '' } = Astro.props;
---

<time datetime={date.toISOString()} class={className}>{formatDate(date)}</time>
